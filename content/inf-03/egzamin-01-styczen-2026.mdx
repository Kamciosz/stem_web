Ten arkusz to Twój pierwszy pełny egzamin do przejścia. Nie czytaj go jak podręcznik — czytaj jak zadanie, które masz 150 minut na rozwiązanie. Potem przeczytaj moje rozwiązanie i porównaj ze swoim. Na końcu — wnioski: co w tej sesji było nowe i na co zwrócić uwagę.

## Arkusz: INF.03-01-26.01-SG (styczeń 2026)

**Temat:** Salon samochodowy — baza `samochody`, strona PHP z tabelą pojazdów, konfiguratorem i kontaktem.

**Czas:** 150 minut. **Język:** PHP. **Baza:** MariaDB/MySQL.

### Plan na 150 minut

Zanim zaczniesz kodować, zobacz plan. 150 minut to dużo, ale tylko jeśli nie utkniesz na jednym zadaniu:

<div className="exam-timeline">
  <div className="exam-timeline-block sql">
    <div className="minutes">25'</div>
    <div className="label">SQL</div>
    <div className="detail">4 zapytania · ~8 pkt</div>
  </div>
  <div className="exam-timeline-block php">
    <div className="minutes">65'</div>
    <div className="label">PHP</div>
    <div className="detail">3 sekcje + baza · ~16 pkt</div>
  </div>
  <div className="exam-timeline-block css-block">
    <div className="minutes">40'</div>
    <div className="label">CSS</div>
    <div className="detail">flex + kolory · ~6 pkt</div>
  </div>
  <div className="exam-timeline-block check">
    <div className="minutes">20'</div>
    <div className="label">Sprawdzenie</div>
    <div className="detail">porównaj z makietą</div>
  </div>
</div>

<Callout type="tip" title="Kolejność ma sens">
SQL rób pierwsze — szybkie punkty. PHP zajmie najwięcej (JOIN + obliczenie + rowspan). Zostaw 20 min na koniec — odpal w przeglądarce i porównaj z makietą punkt po punkcie.
</Callout>

### Zdjęcia z arkusza

Dostajesz 3 pliki graficzne — to one lądują na stronie:

<div className="exam-photos">
  <figure>
    <a href="/img/egzaminy/e01-a1.jpg" target="_blank" rel="noopener">
      <img src="/img/egzaminy/e01-a1.jpg" alt="a1.jpg — niebieskie Audi" />
    </a>
    <figcaption>a1.jpg — sekcja środkowa (kliknij → pełny rozmiar)</figcaption>
  </figure>
  <figure>
    <a href="/img/egzaminy/e01-a2.jpg" target="_blank" rel="noopener">
      <img src="/img/egzaminy/e01-a2.jpg" alt="a2.jpg — białe BMW" />
    </a>
    <figcaption>a2.jpg — sekcja środkowa (kliknij → pełny rozmiar)</figcaption>
  </figure>
  <figure>
    <a href="/img/egzaminy/e01-a3.png" target="_blank" rel="noopener">
      <img src="/img/egzaminy/e01-a3.png" alt="a3.png — czerwony kabriolet" />
    </a>
    <figcaption>a3.png — sekcja kontakt (kliknij → pełny rozmiar)</figcaption>
  </figure>
</div>

### Jak wygląda gotowa strona

<a className="exam-screenshot" href="/img/egzaminy/e01-rozwiazanie.webp" target="_blank" rel="noopener">
  <img src="/img/egzaminy/e01-rozwiazanie.webp" alt="Screenshot gotowej strony — serwis konfiguracji samochodów" />
</a>

To jest cel. Trzy zielone kolumny, ciemnozielony header/footer, tabela z pojazdami po lewej, losowe auta ze zdjęciami w środku, kontakt po prawej.

---

## Baza danych — co dostajesz

Dwie tabele połączone <Term word="klucz_obcy">kluczem obcym</Term>:

<div className="exam-db">
  <div className="exam-db-table">
    <div className="table-name"><span>kolory</span></div>
    <table>
      <tbody>
        <tr>
          <td className="key pk">PK</td>
          <td className="col-name">id</td>
          <td className="col-type">INT A_I</td>
        </tr>
        <tr>
          <td className="key"></td>
          <td className="col-name">nazwa</td>
          <td className="col-type">VARCHAR(20)</td>
        </tr>
        <tr>
          <td className="key"></td>
          <td className="col-name">doplata</td>
          <td className="col-type">DECIMAL(10,2)</td>
        </tr>
      </tbody>
    </table>
    <div className="table-footer">8 kolorów: 4 bazowe (0 zł), 4 metalik (+1000 zł)</div>
  </div>
  <div className="exam-db-table">
    <div className="table-name"><span>pojazdy</span></div>
    <table>
      <tbody>
        <tr>
          <td className="key pk">PK</td>
          <td className="col-name">id</td>
          <td className="col-type">INT A_I</td>
        </tr>
        <tr>
          <td className="key"></td>
          <td className="col-name">marka</td>
          <td className="col-type">VARCHAR(20)</td>
        </tr>
        <tr>
          <td className="key"></td>
          <td className="col-name">model</td>
          <td className="col-type">VARCHAR(20)</td>
        </tr>
        <tr>
          <td className="key"></td>
          <td className="col-name">cena</td>
          <td className="col-type">DECIMAL(15,0)</td>
        </tr>
        <tr>
          <td className="key fk">FK</td>
          <td className="col-name">kolor</td>
          <td className="col-type">→ kolory.id</td>
        </tr>
      </tbody>
    </table>
    <div className="table-footer">62 pojazdy: marki BM/AM, modele alfa/beta/meta</div>
  </div>
</div>

<div className="exam-formula">
  <code>cena_calkowita = pojazdy.cena + kolory.doplata</code>
  <span className="note">(obliczasz w PHP!)</span>
</div>

```sql
CREATE TABLE kolory (
  id      INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  nazwa   VARCHAR(20) NOT NULL,
  doplata DECIMAL(10,2) UNSIGNED DEFAULT NULL
);

CREATE TABLE pojazdy (
  id    INT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  marka VARCHAR(20) NOT NULL,
  model VARCHAR(20) NOT NULL,
  cena  DECIMAL(15,0) UNSIGNED NOT NULL,
  kolor INT UNSIGNED NOT NULL,
  FOREIGN KEY (kolor) REFERENCES kolory(id)
);
```

**Dane:** 8 kolorów (4 bazowe z dopłatą 0, 4 metalik z dopłatą 1000). 62 pojazdy, marki `BM`/`AM`, modele `alfa`/`beta`/`meta`.

## Zadanie 1 — Zapytania SQL

Egzaminator daje 4 zapytania. Oto one z rozwiązaniem:

### Zapytanie 1: 15 najdroższych pojazdów marki BM

```sql
SELECT marka, model, cena
FROM pojazdy
WHERE marka = 'BM'
ORDER BY cena DESC
LIMIT 15;
```

<Callout type="tip" title="Punkty za kolejność">
`ORDER BY` MUSI być przed `LIMIT`. Odwrotna kolejność = błąd składni = 0 punktów za to zapytanie.
</Callout>

### Zapytanie 2: średnia i maksymalna cena modelu "meta"

```sql
SELECT AVG(cena) AS 'Średnia cena', MAX(cena) AS 'Maksymalna cena'
FROM pojazdy
WHERE model = 'meta';
```

Aliasy (`AS`) muszą być **dokładnie** takie jak w poleceniu. Egzaminator porównuje wynik z wzorcem.

### Zapytanie 3: pojazdy "alfa" z danymi koloru (JOIN)

```sql
SELECT marka, model, cena, nazwa, doplata
FROM pojazdy
INNER JOIN kolory ON pojazdy.kolor = kolory.id
WHERE model = 'alfa';
```

<Callout type="exam">
<Term word="JOIN">JOIN</Term> na kluczu obcym to **najczęściej punktowane zapytanie** na egzaminie INF.03. Jeśli nie umiesz pisać JOIN-ów z pamięci — wróć do lekcji "Relacje i JOIN" i ćwicz do skutku.
</Callout>

### Zapytanie 4: 2 losowe pojazdy

```sql
SELECT marka, model, cena
FROM pojazdy
ORDER BY RAND()
LIMIT 2;
```

`ORDER BY RAND()` — sortuje losowo, `LIMIT 2` bierze dwa pierwsze. Proste, ale trzeba znać.

## Zadanie 2 — Skrypt PHP (index.php)

Strona ma 3 sekcje w `main` (flex, każda 33.3%):

<div className="exam-layout">
  <div className="exam-layout-col green">
    <div className="col-title">#lewa</div>
    <div className="col-body">
      Tabela: model='alfa'<br/>
      JOIN kolory + pojazdy<br/>
      <strong>cena = cena + doplata</strong><br/>
      <span className="exam-badge purple">obliczenie w PHP!</span><br/>
      <span className="col-note">tr:hover → ForestGreen</span>
    </div>
  </div>
  <div className="exam-layout-col green">
    <div className="col-title">#srodkowa</div>
    <div className="col-body">
      2 losowe pojazdy<br/>
      <span className="exam-badge blue">a1.jpg</span>{' '}
      <span className="exam-badge gray">a2.jpg</span><br/>
      <strong>rowspan=2</strong><br/>
      <span className="exam-badge purple">RAND() LIMIT 2</span><br/>
      <span className="col-note">img width: 95%</span>
    </div>
  </div>
  <div className="exam-layout-col green">
    <div className="col-title">#prawa</div>
    <div className="col-body">
      Kontakt<br/>
      tel: 111 222 444<br/>
      <span className="exam-badge red">a3.png</span><br/>
      <span className="col-note">statyczne HTML</span>
    </div>
  </div>
</div>

### Połączenie z bazą

```php
<?php
$conn = new mysqli("localhost", "root", "", "samochody");
$conn->set_charset("utf8");
```

<Callout type="warning" title="set_charset">
Bez `set_charset("utf8")` polskie znaki w bazie (np. "niebieski") mogą się wyświetlić jako krzaki. Egzaminator to widzi i odejmuje punkt.
</Callout>

### Sekcja #lewa — tabela pojazdów "alfa" z ceną całkowitą

Tu łączysz `pojazdy` z `kolory` i obliczasz cenę w PHP:

```php
$query = "SELECT marka, model, cena, nazwa, doplata
          FROM pojazdy
          INNER JOIN kolory ON kolor = kolory.id
          WHERE model = 'alfa'";
$result = $conn->query($query);

echo "<table>";
echo "<tr><th>Marka</th><th>Model</th><th>Kolor</th><th>Cena całkowita</th></tr>";

while ($row = $result->fetch_assoc()) {
    $cena_calkowita = $row['cena'] + $row['doplata'];
    echo "<tr>";
    echo "<td>" . $row['marka'] . "</td>";
    echo "<td>" . $row['model'] . "</td>";
    echo "<td>" . $row['nazwa'] . "</td>";
    echo "<td>" . $cena_calkowita . "</td>";
    echo "</tr>";
}
echo "</table>";
```

**Dlaczego obliczenie w PHP a nie w SQL?** Bo polecenie mówi "oblicz cenę całkowitą w skrypcie". Egzaminator sprawdza, CZY robisz to w PHP. Gdybyś zrobił `SELECT cena + doplata AS cena_calkowita` w SQL — technicznie działa, ale tracisz punkt za "realizację w skrypcie".

### Sekcja #srodkowa — 2 losowe pojazdy z obrazkami

```php
$query = "SELECT marka, model, cena FROM pojazdy ORDER BY RAND() LIMIT 2";
$result = $conn->query($query);
$nr = 1;

while ($row = $result->fetch_assoc()) {
    echo "<img src='a{$nr}.jpg' alt='Pojazd {$nr}'>";
    echo "<table>";
    echo "<tr><td rowspan='2'>" . $row['cena'] . "</td>";
    echo "<td>Marka: " . $row['marka'] . "</td></tr>";
    echo "<tr><td>Model: " . $row['model'] . "</td></tr>";
    echo "</table>";
    $nr++;
}
```

<Callout type="tip" title="rowspan/colspan">
`rowspan='2'` oznacza, że komórka zajmuje 2 wiersze. Egzaminator patrzy na to w źródle HTML — jeśli brakuje, tracisz punkt za "zgodność z makietą".
</Callout>

### Sekcja #prawa — statyczna

```php
echo "<p>Telefon: 111 222 444</p>";
echo "<img src='a3.png' alt='Kontakt'>";
```

### Zamknięcie

```php
$conn->close();
```

## Zadanie 3 — CSS (styl.css)

Kluczowe reguły, które egzaminator sprawdza:

```css
* {
    text-align: center;
    font-family: Helvetica, sans-serif;
}

header, footer {
    background-color: DarkGreen;
    color: white;
}

nav {
    background-color: ForestGreen;
}

main {
    display: flex;
}

section {
    background-color: LightGreen;
    width: 33.3%;
    height: 550px;
    overflow: auto;
}

table {
    width: 90%;
    border-collapse: collapse;
}

table, th, td {
    border: 1px solid DarkGreen;
}

#lewa tr:hover {
    background-color: ForestGreen;
}

#srodkowa img {
    width: 95%;
    border-radius: 8px;
}
```

<Callout type="exam">
`display: flex` na `main` + `width: 33.3%` na `section` = trzy kolumny obok siebie. To najczęstszy layout na egzaminie INF.03. Naucz się go na pamięć.
</Callout>

## Omówienie punktacji

| Element | Punkty | Na co patrzy egzaminator |
|---------|--------|--------------------------|
| Zapytania SQL (4 szt.) | ~8 pkt | Poprawność składni, aliasy, kolejność klauzul |
| Połączenie z bazą | ~2 pkt | mysqli, charset, close() |
| Skrypt #1 (JOIN + obliczenie) | ~6 pkt | JOIN w PHP, obliczenie w PHP (nie SQL), pętla while |
| Skrypt #2 (losowanie) | ~4 pkt | ORDER BY RAND(), rowspan, dynamiczne obrazki |
| HTML struktura | ~4 pkt | Semantyka (header/nav/main/footer), zgodność z makietą |
| CSS | ~6 pkt | flex, kolory, hover, border-collapse, overflow |

**Łącznie:** ~30 punktów. Próg zdania: 75% = 22-23 punkty.

## Wnioski z sesji styczeń 2026

Co nowego / nietypowego w porównaniu z poprzednimi latami:

1. **Obliczenie w PHP zamiast SQL** — wcześniej (2024-2025) cena finalna była liczona w zapytaniu SQL (`SELECT cena + doplata`). Teraz egzaminator wymaga, żebyś to zrobił w pętli PHP. Czytaj polecenie dosłownie.

2. **ORDER BY RAND()** — pojawiło się po raz pierwszy w sesji głównej (wcześniej było tylko w próbnych). Zapamiętaj składnię.

3. **rowspan w dynamicznej tabeli** — generujesz HTML z `rowspan` wewnątrz pętli `while`. To trudniejsze niż statyczny rowspan, bo musisz pilnować, który wiersz ma `rowspan` a który nie.

4. **Motyw kolorystyczny z nazwami CSS** — `DarkGreen`, `ForestGreen`, `LightGreen`. Egzaminator akceptuje zarówno nazwy jak i hex, ale nazwy są szybsze do wpisania i mniej podatne na literówki.

5. **overflow: auto na section** — gdy tabela jest dłuższa niż 550px, pojawia się scroll. Bez tego treść wylewa się poza sekcję i tracisz punkt za "zgodność z makietą".
