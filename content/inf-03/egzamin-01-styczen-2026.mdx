<ExamGuideShell>

<ExamHero
  kicker="INF.03 · Styczeń 2026 · Arkusz 01"
  title="Arkusz 01 — Portal samochodowy"
  description="Przewodnik po rozwiązaniu w kolejności pracy. Plan, materiały, błędy, kod i kontrola — szczegóły rozwijasz tylko gdy ich potrzebujesz."
  rule="SQL → PHP → CSS → Kontrola"
  stats={[
    { label: "Czas", value: "150 min" },
    { label: "Technologie", value: "PHP, SQL, CSS" },
    { label: "Cel", value: "Strona salonu aut" },
    { label: "Próg", value: "~22/30 pkt" }
  ]}
/>

<ExamQuickNav
  items={[
    { href: "#plan", label: "Plan" },
    { href: "#materialy", label: "Materiały" },
    { href: "#strategia", label: "Strategia" },
    { href: "#bledy", label: "Błędy" },
    { href: "#kod", label: "Kod" },
    { href: "#punktacja", label: "Punktacja" },
    { href: "#checklista", label: "Checklista" }
  ]}
/>

<ExamSection
  id="plan"
  eyebrow="Plan"
  title="Co trzeba zrobić"
  lead="Arkusz jest prosty, ale ma dużo małych wymagań. Rozwijaj etapy po kolei."
>
  <ExamPlan
    steps={[
      {
        title: "Baza danych",
        summary: "Dwie tabele i relacja przez kolor.",
        points: "Tabele kolory i pojazdy, klucz główny, klucz obcy.",
        mistake: "Brak relacji pojazdy.kolor → kolory.id.",
        details: <div className="exam-copy"><code>kolory.id</code> to <ExamTooltip word="PRIMARY KEY">PRIMARY KEY</ExamTooltip>, <code>pojazdy.kolor</code> to <ExamTooltip word="klucz obcy">klucz obcy</ExamTooltip>. Bez tego <ExamTooltip word="JOIN">JOIN</ExamTooltip> nie działa.</div>
      },
      {
        title: "SQL",
        summary: "Cztery zapytania przed PHP.",
        points: "Filtrowanie, agregacja, JOIN, losowanie, kolejność ORDER BY i LIMIT.",
        mistake: "ORDER BY i LIMIT w złej kolejności, JOIN bez ON.",
        details: <div className="exam-copy">Najpierw sprawdź zapytania w bazie, dopiero potem wklejaj do PHP.</div>
      },
      {
        title: "PHP",
        summary: "Połączenie, pętle, obliczenia, tabela.",
        points: "mysqli, <code>set_charset</code>, cena całkowita w PHP, dwa losowe pojazdy.",
        mistake: "Cena policzona w SQL lub brak <code>rowspan=\"2\"</code>.",
        details: <div className="exam-copy">Jeśli polecenie wymaga <ExamTooltip word="rowspan">rowspan</ExamTooltip>, ma być atrybut w kodzie — nie podobny wygląd.</div>
      },
      {
        title: "CSS",
        summary: "Odtworzenie makiety.",
        points: "Trzy kolumny, kolory z polecenia, tabela, hover, overflow, proporcje sekcji.",
        mistake: "Ładniejsza strona niż makieta. Tracisz punkty.",
        details: <div className="exam-copy"><code>main</code>: <ExamTooltip word="display: flex">display: flex</ExamTooltip>, sekcje <code>33.3%</code>, tabela <ExamTooltip word="overflow">overflow</ExamTooltip>.</div>
      },
      {
        title: "Kontrola",
        summary: "Ostatnie 20 minut na porównanie z makietą.",
        points: "Ścieżki obrazków, liczba aut, tabela, polskie znaki, wygląd.",
        mistake: "Oddanie bez odpalenia strony.",
        details: <div className="exam-copy">Sprawdzasz wymaganie po wymaganiu z checklisty.</div>
      }
    ]}
  />
</ExamSection>

<ExamSection
  id="materialy"
  eyebrow="Materiały"
  title="Pliki z arkusza"
  lead="Trzy obrazki i makieta końcowa. Otwórz w nowej karcie, porównaj ścieżki."
>
  <ExamMaterials
    materials={[
      { src: "/img/egzaminy/e01-a1.jpg", alt: "a1.jpg — niebieskie Audi", title: "e01-a1.jpg", caption: "sekcja środkowa" },
      { src: "/img/egzaminy/e01-a2.jpg", alt: "a2.jpg — białe BMW", title: "e01-a2.jpg", caption: "drugi pojazd" },
      { src: "/img/egzaminy/e01-a3.png", alt: "a3.png — czerwony kabriolet", title: "e01-a3.png", caption: "kontakt" }
    ]}
    result={{ src: "/img/egzaminy/e01-rozwiazanie.webp", alt: "Makieta końcowa INF.03-01-26.01", title: "Makieta końcowa", caption: "do odtworzenia 1:1" }}
  />
</ExamSection>

<ExamSection
  id="strategia"
  eyebrow="Strategia"
  title="150 minut, kolejność"
  lead="Najpierw działające dane, potem wygląd."
>
  <ExamTimeline
    steps={[
      { time: "0–25 min", title: "SQL", body: "Cztery zapytania sprawdzone w bazie.", tag: "łatwe punkty" },
      { time: "25–90 min", title: "PHP i baza", body: "Połączenie, charset, pętle, cena, losowanie, tabela z rowspan.", tag: "tu się oblewa" },
      { time: "90–130 min", title: "CSS pod makietę", body: "Trzy kolumny, kolory, hover, overflow.", tag: "dopięcie" },
      { time: "130–150 min", title: "Kontrola", body: "Porównanie z makietą punkt po punkcie.", tag: "ostatnie punkty" }
    ]}
  />
</ExamSection>

<ExamSection
  id="bledy"
  eyebrow="Błędy"
  title="Co najczęściej kosztuje punkty"
  lead="Jedna lista. Sprawdź przed oddaniem."
>
  <ExamWarnings
    items={[
      { title: "Brak set_charset", impact: "Polskie znaki się rozsypują.", check: "PHP, zaraz po new mysqli()." },
      { title: "Zły JOIN", impact: "Błędne lub zduplikowane wiersze.", check: "Zapytanie 3 i lewa sekcja PHP." },
      { title: "Cena policzona w SQL", impact: "Polecenie wymaga obliczenia w PHP.", check: "Pętla w lewej sekcji." },
      { title: "Brak rowspan=\"2\"", impact: "Tabela środkowa niezgodna z makietą.", check: "HTML w sekcji środkowej." },
      { title: "ORDER BY / LIMIT", impact: "Złe sortowanie lub losowanie.", check: "Zapytanie 1 i 4." },
      { title: "Ścieżki obrazków", impact: "Brak obrazków na stronie.", check: "a1.jpg, a2.jpg, a3.png w katalogu i w src." },
      { title: "CSS niezgodny z makietą", impact: "Twój projekt zamiast odpowiedzi na arkusz.", check: "Kolory, kolumny, wysokość, overflow, hover." }
    ]}
  />
</ExamSection>

<ExamSection
  id="kod"
  eyebrow="Kod"
  title="Rozwijaj fragmenty na bieżąco"
  lead="Przeczytaj opis i błąd, kod pokaż gdy chcesz porównać."
>
  <h3>SQL</h3>
  <ExamCodeGrid>
    <ExamCodeBlock
      language="SQL"
      title="Zapytanie 1 — 15 najdroższych BM"
      description="Filtr marki, sort malejąco, limit 15."
      mistake="Brak DESC lub LIMIT bez ORDER BY."
      code={`SELECT marka, model, cena
FROM pojazdy
WHERE marka = 'BM'
ORDER BY cena DESC
LIMIT 15;}
    />
    <ExamCodeBlock
      language="SQL"
      title="Zapytanie 2 — średnia i max meta"
      description="Dwie agregacje z aliasami z polecenia."
      mistake="Złe aliasy lub brak WHERE."
      code={SELECT AVG(cena) AS 'Średnia cena', MAX(cena) AS 'Maksymalna cena'
FROM pojazdy
WHERE model = 'meta';}
    />
    <ExamCodeBlock
      language="SQL"
      title="Zapytanie 3 — alfa z kolorem"
      description="JOIN z warunkiem ON na kluczu obcym."
      mistake="JOIN bez ON daje cartesian product."
      code={SELECT marka, model, cena, nazwa, doplata
FROM pojazdy
INNER JOIN kolory ON pojazdy.kolor = kolory.id
WHERE model = 'alfa';}
    />
    <ExamCodeBlock
      language="SQL"
      title="Zapytanie 4 — dwa losowe"
      description="RAND() i LIMIT 2."
      mistake="LIMIT przed ORDER BY."
      code={SELECT marka, model, cena
FROM pojazdy
ORDER BY RAND()
LIMIT 2;}
    />
  </ExamCodeGrid>

  <h3>PHP</h3>
  <ExamCodeGrid>
    <ExamCodeBlock
      language="PHP"
      title="Połączenie z bazą"
      description="mysqli + charset od razu po połączeniu."
      mistake="Brak charset — lokalnie działa, na egzaminie nie."
      code={<?php
$conn = new mysqli("localhost", "root", "", "samochody");
$conn->set_charset;}
    />
    <ExamCodeBlock
      language="PHP"
      title="Lewa sekcja — tabela alfa"
      description="JOIN, pętla, cena całkowita liczona w PHP."
      mistake="Cena w SELECT zamiast w pętli — wymaganie niespełnione."
      code={$query = "SELECT marka, model, cena, nazwa, doplata
          FROM pojazdy
          INNER JOIN kolory ON pojazdy.kolor = kolory.id
          WHERE model = 'alfa'";
$result = $conn->query($query);

echo "<table>";
echo "<tr><th>Marka</th><th>Model</th><th>Kolor</th><th>Cena całkowita</th></tr>";

while ($row = $result->fetch_assoc()) {
    $cena_calkowita = $row['cena'] + $row['doplata'];
    echo "<tr>";
    echo "<td>" . $row['marka'] . "</td>";
    echo "<td>" . $row['model'] . "</td>";
    echo "<td>" . $row['nazwa'] . "</td>";
    echo "<td>" . $cena_calkowita . "</td>";
    echo "</tr>";
}

echo "</table>";}
    />
    <ExamCodeBlock
      language="PHP"
      title="Środek — losowanie i rowspan"
      description="Dwa auta, dwa obrazki, tabela z rowspan='2'."
      mistake="Brak rowspan — niespełnione wymaganie z makiety."
      code={$query = "SELECT marka, model, cena FROM pojazdy ORDER BY RAND() LIMIT 2";
$result = $conn->query($query);
$nr = 1;

while ($row = $result->fetch_assoc()) {
    echo "<img src='a{$nr}.jpg' alt='Pojazd {$nr}'>";
    echo "<table>";
    echo "<tr><td rowspan='2'>" . $row['cena'] . "</td>";
    echo "<td>Marka: " . $row['marka'] . "</td></tr>";
    echo "<tr><td>Model: " . $row['model'] . "</td></tr>";
    echo "</table>";
    $nr++;
}}
    />
    <ExamCodeBlock
      language="PHP"
      title="Prawa sekcja i zamknięcie"
      description="Kontakt, obrazek a3.png, close()."
      mistake="Zła nazwa pliku lub brak close()."
      code={echo "<p>Telefon: 111 222 444</p>";
echo "<img src='a3.png' alt='Kontakt'>";

$conn->close();}
    />
  </ExamCodeGrid>

  <h3>CSS</h3>
  <ExamCodeGrid>
    <ExamCodeBlock
      language="CSS"
      title="Układ zgodny z makietą"
      description="Trzy kolumny, kolory z polecenia, hover, overflow."
      mistake="Brak overflow lub sekcje nie po 33.3%."
      code={* {
    text-align: center;
    font-family: Helvetica, sans-serif;
}

header, footer {
    background-color: DarkGreen;
    color: white;
}

nav {
    background-color: ForestGreen;
}

main {
    display: flex;
}

section {
    background-color: LightGreen;
    width: 33.3%;
    height: 550px;
    overflow: auto;
}

table {
    width: 90%;
    border-collapse: collapse;
}

table, th, td {
    border: 1px solid DarkGreen;
}

#lewa tr:hover {
    background-color: ForestGreen;
}

#srodkowa img {
    width: 95%;
    border-radius: 8px;
}`}
    />
  </ExamCodeGrid>
</ExamSection>

<ExamSection
  id="punktacja"
  eyebrow="Punktacja"
  title="Co sprawdza egzaminator"
  lead="Wartości orientacyjne. Pokazują priorytety — SQL i PHP ważą najwięcej."
>
  <ExamScoring
    items={[
      { area: "SQL", points: "~8 pkt", criterion: "Składnia, aliasy, JOIN, kolejność ORDER BY i LIMIT." },
      { area: "Połączenie z bazą", points: "~2 pkt", criterion: "mysqli, baza samochody, UTF-8, close()." },
      { area: "PHP lewa sekcja", points: "~6 pkt", criterion: "JOIN, pętla, tabela, cena liczona w PHP." },
      { area: "PHP środek", points: "~4 pkt", criterion: "Losowanie dwóch pojazdów, obrazki, rowspan." },
      { area: "HTML/CSS", points: "~10 pkt", criterion: "Układ zgodny z makietą." }
    ]}
  />
</ExamSection>

<ExamSection
  id="checklista"
  eyebrow="Checklista"
  title="Przed oddaniem"
  lead="Lokalna, nie zapisuje się nigdzie. Sprawdź konkretne wymagania."
>
  <ExamChecklist
    items={[
      { label: <>4 zapytania SQL działają.</> },
      { label: <>PHP łączy się z bazą <code>samochody</code>.</> },
      { label: <>Ustawiono UTF-8 przez <code>set_charset</code>.</> },
      { label: <>Cena całkowita liczona w PHP.</> },
      { label: <>Dokładnie 2 losowe pojazdy.</> },
      { label: <>Tabela używa <code>rowspan="2"</code>.</> },
      { label: <><code>a1.jpg</code>, <code>a2.jpg</code>, <code>a3.png</code> w dobrych miejscach.</> },
      { label: <><code>main</code> ma <code>display: flex</code>.</> },
      { label: <>Sekcje po <code>33.3%</code>.</> },
      { label: <>Wygląd zgadza się z makietą.</> }
    ]}
  />
</ExamSection>

<div className="exam-verdict final">
  <div className="exam-verdict-label">cel</div>
  <div className="exam-copy">
    Zgodność z poleceniem, nie kreatywność. Punkty są za spełnienie wymagań — nie za to, że kod „prawie tak samo wygląda”.
  </div>
</div>

</ExamGuideShell>
