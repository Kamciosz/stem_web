**Arkusz:** INF.03-09-26.01-SG (styczeń 2026)
**Technologia:** PHP + JS + SQL · **Czas:** 150 minut
**Archiwum:** `pliki15.7z` · **Hasło:** `P@znoKcie?`

<Callout type="exam" title="Podejście do egzaminu">

Ten arkusz omawiam jako **zadanie egzaminacyjne**, nie jako lekcję technologii. Masz tu: gotowe rozwiązanie, wyjaśnienie dlaczego tak, i tipy punktowe. Rozwiąż najpierw sam, potem porównaj.

</Callout>

## JavaScript — co jest oceniane

JS w tych arkuszach działa w przeglądarce: obsługuje kliknięcia lub `onmouseover`, zmienia DOM, liczy wynik albo dodaje obraz. Używaj `const` dla wartości nieprzypisywanych ponownie i `let` dla liczników pętli. Nie używaj starego deklarowania zmiennych w nowych rozwiązaniach.

## Skrypt 1 — 10 obrazów w pętli (JS lub PHP)

### Wersja JavaScript:

<ExamCodeGrid>
  <ExamCodeBlock
    language="JAVASCRIPT"
    title="Kod JavaScript"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`const sekcja3 = document.getElementById("sekcja3");
for (let i = 1; i <= 10; i++) {
    const img = document.createElement("img");
    img.src = i + ".jpg";
    img.className = "wzory";
    img.title = i;
    sekcja3.appendChild(img);
}`}
  />
</ExamCodeGrid>

### Wersja PHP:

<ExamCodeGrid>
  <ExamCodeBlock
    language="PHP"
    title="Kod PHP"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`for ($i = 1; $i <= 10; $i++) {
    echo "<img src='$i.jpg' class='wzory' title='$i'>";
}`}
  />
</ExamCodeGrid>

## Skrypt 2 — przełączanie sekcji (JavaScript onmouseover)

<ExamCodeGrid>
  <ExamCodeBlock
    language="JAVASCRIPT"
    title="Kod JavaScript"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`function pokaz(nr) {
    const sekcje = document.querySelectorAll("section");
    const przyciski = document.querySelectorAll("nav button");

    for (let i = 0; i < sekcje.length; i++) {
        sekcje[i].style.display = "none";
        przyciski[i].style.backgroundColor = "Crimson";
    }

    sekcje[nr].style.display = "block";
    przyciski[nr].style.backgroundColor = "Salmon";
}`}
  />
</ExamCodeGrid>

HTML przycisków w nav:

<ExamCodeGrid>
  <ExamCodeBlock
    language="HTML"
    title="Kod HTML"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`<button onmouseover="pokaz(0)">Kolor</button>
<button onmouseover="pokaz(1)">Kształt</button>
<button onmouseover="pokaz(2)">Wzór</button>`}
  />
</ExamCodeGrid>

<Callout type="warning" title="onmouseover, nie onclick">
Polecenie mówi "zdarzenie najechania kursorem" — to `onmouseover`. Użycie `onclick` = utrata punktu za "działanie witryny".
</Callout>

## Minimum do zapamiętania z JS

- Elementy DOM pobieraj przez `const`, np. `const wynik = document.getElementById("wynik")`.
- Liczniki pętli zapisuj jako `let`, np. `for (let i = 1; i <= 10; i++)`.
- `input[type=file].value` może zwracać `C:\fakepath\plik.png` — wytnij nazwę pliku przed użyciem.
- `element.src` zwykle jest pełnym URL-em, więc przy nazwach plików używaj `includes()`.
