**Arkusz:** INF.03-09-26.01-SG (styczeń 2026)
**Technologia:** PHP + JS + SQL · **Czas:** 150 minut
**Archiwum:** `pliki15.7z` · **Hasło:** `P@znoKcie?`

<Callout type="exam" title="Podejście do egzaminu">

Ten arkusz omawiam jako **zadanie egzaminacyjne**, nie jako lekcję technologii. Masz tu: gotowe rozwiązanie, wyjaśnienie dlaczego tak, i tipy punktowe. Rozwiąż najpierw sam, potem porównaj.

</Callout>

## PHP — co jest oceniane

PHP w INF.03 działa po stronie serwera: łączy się z MySQL, wykonuje zapytania, odczytuje GET/POST, generuje HTML i zamyka połączenie. Nie mieszaj tego z JavaScriptem — JS nie połączy się z bazą w tych arkuszach.

## Zapytania SQL — gotowe rozwiązania

### Zapytanie 1: ważni klienci

<ExamCodeGrid>
  <ExamCodeBlock
    language="SQL"
    title="Kod SQL"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`SELECT imie, kolor, cena FROM klienci WHERE topKlient = 1;`}
  />
</ExamCodeGrid>

### Zapytanie 2: klienci z dnia 2024-05-23

<ExamCodeGrid>
  <ExamCodeBlock
    language="SQL"
    title="Kod SQL"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`SELECT imie, cena FROM klienci WHERE data = '2024-05-23';`}
  />
</ExamCodeGrid>

### Zapytanie 3: klienci z kształtem migdał (JOIN)

<ExamCodeGrid>
  <ExamCodeBlock
    language="SQL"
    title="Kod SQL"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`SELECT k.imie, k.kolor, ks.nazwa
FROM klienci k
JOIN ksztalty ks ON k.ksztalt = ks.id
WHERE ks.nazwa = 'migdał';`}
  />
</ExamCodeGrid>

### Zapytanie 4: dodanie kolumny doplata

<ExamCodeGrid>
  <ExamCodeBlock
    language="SQL"
    title="Kod SQL"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`ALTER TABLE wzory ADD COLUMN doplata INT;`}
  />
</ExamCodeGrid>

<Callout type="exam">
`ALTER TABLE ... ADD COLUMN` — to jedyny arkusz w tej sesji, który wymaga modyfikacji struktury tabeli (DDL zamiast DML). Musisz znać różnicę między SELECT/INSERT (DML) a ALTER/CREATE (DDL).
</Callout>


## PHP — opcjonalna pętla obrazów wzorów

<ExamCodeGrid>
  <ExamCodeBlock
    language="PHP"
    title="Wersja PHP skryptu 1"
    description="Jeżeli wybierasz wariant PHP, generujesz 10 obrazów w pętli po stronie serwera."
    mistake="Nie pomyl nazw plików: wzory są numerowane od 1.jpg do 10.jpg, a pętla musi objąć wszystkie 10 elementów."
    code={`<?php
for ($i = 1; $i <= 10; $i++) {
    echo "<img src='$i.jpg' class='wzory' title='$i' alt='Wzór $i'>";
}
?>`}
  />
</ExamCodeGrid>

<Callout type="exam" title="PHP czy JavaScript?">
Arkusz dopuszcza wariant generowania obrazów w PHP albo w JavaScripcie. Wybierz jeden i zrób go konsekwentnie. Etap PHP pokazuje wariant serwerowy, a etap JavaScript pokazuje wariant DOM.
</Callout>

## Minimum do zapamiętania z PHP

- Po `mysqli_connect()` ustaw kodowanie (`SET CHARSET utf8` albo odpowiednik z arkusza).
- Najpierw testuj SQL w bazie, potem wklejaj do PHP.
- Dane z formularza czytaj przez `$_POST`/`$_GET` zgodnie z poleceniem.
- `while + mysqli_fetch_array/fetch_assoc` to podstawowy wzorzec renderowania wyników.
- Zamknij połączenie, jeśli arkusz tego wymaga.
