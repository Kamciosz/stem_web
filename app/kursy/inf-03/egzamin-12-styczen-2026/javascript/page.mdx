**Arkusz:** INF.03-12-26.01-SG (styczeń 2026)
**Technologia:** PHP + JS + SQL · **Czas:** 150 minut
**Archiwum:** `pliki16.7z` · **Hasło:** `<Studio&T@tu>`

<Callout type="exam" title="Podejście do egzaminu">

Ten arkusz omawiam jako **zadanie egzaminacyjne**, nie jako lekcję technologii. Masz tu: gotowe rozwiązanie, wyjaśnienie dlaczego tak, i tipy punktowe. Rozwiąż najpierw sam, potem porównaj.

</Callout>



## Mapa technologii INF.03 dla tego arkusza

INF.03 obejmuje tworzenie stron i aplikacji internetowych oraz baz danych. Dlatego w tym kursie rozbijamy front-end na konkretne technologie zamiast trzymać wszystko w jednym dziale. Ucz się etapami:

- **HTML** — struktura dokumentu, nazwy plików, formularze, linki, grafika i semantyczne bloki.
- **JavaScript** — zdarzenia użytkownika, DOM, obliczenia po stronie klienta, createElement/appendChild albo przełączanie widoku.

<Callout type="exam" title="Dlaczego osobno HTML, PHP i JavaScript?">
HTML opisuje strukturę strony, CSS wygląd, PHP działa po stronie serwera i zwykle łączy się z bazą, a JavaScript działa w przeglądarce. Na egzaminie punkt jest za wymaganie, nie za nazwę działu — dlatego technologię trzeba rozpoznać po tym, gdzie kod się wykonuje.
</Callout>

## Jak się z tego uczyć

Nie przepisuj kodu jednym blokiem. Czytaj go w trzech przejściach:

1. **Wejście** — skąd biorą się dane: baza, formularz, pliki graficzne albo zdarzenie JavaScript.
2. **Przetwarzanie** — co kod robi z danymi: filtruje, liczy, zapisuje, wybiera albo wyświetla.
3. **Wyjście** — co ma zobaczyć egzaminator: tabela, lista, komunikat, obraz albo zmieniony fragment strony.

<Callout type="warning" title="Nie ucz się tego jak gotowca">
Gotowiec bez rozumienia działa tylko do pierwszej zmiany w arkuszu. Na egzaminie rozpoznajesz wzorzec: połączenie z bazą, pętla po wyniku, formularz POST, obliczenie, INSERT, zdarzenie JS albo CSS pod makietę.
</Callout>

## JavaScript — co jest oceniane

JS w tych arkuszach działa w przeglądarce: obsługuje kliknięcia lub `onmouseover`, zmienia DOM, liczy wynik albo dodaje obraz. Używaj `const` dla wartości nieprzypisywanych ponownie i `let` dla liczników pętli. Nie używaj starego deklarowania zmiennych w nowych rozwiązaniach.

## Skrypt JavaScript — dodawanie wzoru do galerii

<ExamCodeGrid>
  <ExamCodeBlock
    language="JAVASCRIPT"
    title="Kod JavaScript"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`function dodajWzor() {
    // Pobranie danych z kontrolek
    const wzor = document.getElementById("wzor").value;
    const plikInput = document.getElementById("plik");
    const kolor = document.getElementById("kolor").value;
    const cena = document.getElementById("cena").value;

    // Ustalenie nazwy pliku (bez ścieżki)
    const pelnaSciezka = plikInput.value;
    const nazwaPliku = pelnaSciezka.split("\\").pop();

    // Komunikat
    alert("Wzór: " + nazwaPliku + ", kolor " + kolor + " w cenie " + cena + " zł");

    // Utworzenie i dodanie obrazu do sekcji
    const img = document.createElement("img");
    img.src = nazwaPliku;
    img.alt = nazwaPliku;
    img.className = "miniatura";

    const sekcja = document.querySelector("section");
    sekcja.appendChild(img);
}`}
  />
</ExamCodeGrid>

<Callout type="warning" title="Ścieżka pliku w input file">
`input[type=file].value` zwraca `C:\fakepath\plik.png`. Musisz wyciąć samą nazwę pliku: `split("\\").pop()` lub `split("/").pop()`. To pułapka z polecenia — jest nawet podpowiedź w arkuszu: "JS Value property: C:\fakepath\fileName.png".
</Callout>

## Minimum do zapamiętania z JS

- Elementy DOM pobieraj przez `const`, np. `const wynik = document.getElementById("wynik")`.
- Liczniki pętli zapisuj jako `let`, np. `for (let i = 1; i <= 10; i++)`.
- `input[type=file].value` może zwracać `C:\fakepath\plik.png` — wytnij nazwę pliku przed użyciem.
- `element.src` zwykle jest pełnym URL-em, więc przy nazwach plików używaj `includes()`.
