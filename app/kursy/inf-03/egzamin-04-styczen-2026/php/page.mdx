**Arkusz:** INF.03-04-26.01-SG (styczeń 2026)
**Technologia:** PHP + SQL · **Czas:** 150 minut
**Archiwum:** `pliki3-2.7z` · **Hasło:** `Z@Rok!Matur@`

<Callout type="exam" title="Podejście do egzaminu">

Ten arkusz omawiam jako **zadanie egzaminacyjne**, nie jako lekcję technologii. Masz tu: gotowe rozwiązanie, wyjaśnienie dlaczego tak, i tipy punktowe. Rozwiąż najpierw sam, potem porównaj.

</Callout>



## Mapa technologii INF.03 dla tego arkusza

INF.03 obejmuje tworzenie stron i aplikacji internetowych oraz baz danych. Dlatego w tym kursie rozbijamy front-end na konkretne technologie zamiast trzymać wszystko w jednym dziale. Ucz się etapami:

- **HTML** — struktura dokumentu, nazwy plików, formularze, linki, grafika i semantyczne bloki.
- **PHP** — połączenie z bazą, mysqli/charset, pętle po wyniku, GET/POST, obliczenia lub zapis danych.

<Callout type="exam" title="Dlaczego osobno HTML, PHP i JavaScript?">
HTML opisuje strukturę strony, CSS wygląd, PHP działa po stronie serwera i zwykle łączy się z bazą, a JavaScript działa w przeglądarce. Na egzaminie punkt jest za wymaganie, nie za nazwę działu — dlatego technologię trzeba rozpoznać po tym, gdzie kod się wykonuje.
</Callout>

## Jak się z tego uczyć

Nie przepisuj kodu jednym blokiem. Czytaj go w trzech przejściach:

1. **Wejście** — skąd biorą się dane: baza, formularz, pliki graficzne albo zdarzenie JavaScript.
2. **Przetwarzanie** — co kod robi z danymi: filtruje, liczy, zapisuje, wybiera albo wyświetla.
3. **Wyjście** — co ma zobaczyć egzaminator: tabela, lista, komunikat, obraz albo zmieniony fragment strony.

<Callout type="warning" title="Nie ucz się tego jak gotowca">
Gotowiec bez rozumienia działa tylko do pierwszej zmiany w arkuszu. Na egzaminie rozpoznajesz wzorzec: połączenie z bazą, pętla po wyniku, formularz POST, obliczenie, INSERT, zdarzenie JS albo CSS pod makietę.
</Callout>

## PHP — co jest oceniane

PHP w INF.03 działa po stronie serwera: łączy się z MySQL, wykonuje zapytania, odczytuje GET/POST, generuje HTML i zamyka połączenie. Nie mieszaj tego z JavaScriptem — JS nie połączy się z bazą w tych arkuszach.

## Zapytania SQL — gotowe rozwiązania

Baza: `matura`. Plik SQL: `egzamin.sql`.

### Zapytanie 1: uczniowie ze szkoły T3

<ExamCodeGrid>
  <ExamCodeBlock
    language="SQL"
    title="Kod SQL"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`SELECT id, imie, nazwisko FROM maturzysta
WHERE szkola = 'T3'
ORDER BY nazwisko ASC;`}
  />
</ExamCodeGrid>

### Zapytanie 2: unikalne przedmioty

<ExamCodeGrid>
  <ExamCodeBlock
    language="SQL"
    title="Kod SQL"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`SELECT DISTINCT przedmiot FROM arkusz;`}
  />
</ExamCodeGrid>

### Zapytanie 3: najstarszy i najmłodszy arkusz

<ExamCodeGrid>
  <ExamCodeBlock
    language="SQL"
    title="Kod SQL"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`SELECT MIN(rok) AS 'Najstarszy', MAX(rok) AS 'Najmłodszy' FROM arkusz;`}
  />
</ExamCodeGrid>

### Zapytanie 4: maturzysta z najlepszą średnią

<ExamCodeGrid>
  <ExamCodeBlock
    language="SQL"
    title="Kod SQL"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`SELECT id_maturzysta, AVG(punkty) AS Wynik
FROM wyniki
GROUP BY id_maturzysta
ORDER BY Wynik DESC
LIMIT 1;`}
  />
</ExamCodeGrid>

<Callout type="exam">
Zapytanie 4 łączy GROUP BY + ORDER BY + LIMIT — trzy klauzule naraz. Alias `Wynik` w ORDER BY działa w MySQL/MariaDB (w innych bazach nie zawsze). Na egzaminie INF.03 używasz MySQL, więc jest OK.
</Callout>

### Zapytanie 5: wyniki maturzysty id=31 z JOIN

<ExamCodeGrid>
  <ExamCodeBlock
    language="SQL"
    title="Kod SQL"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`SELECT a.rok, a.sesja, a.przedmiot, w.punkty
FROM wyniki w
JOIN arkusz a ON w.id_arkusz = a.id
WHERE w.id_maturzysta = 31;`}
  />
</ExamCodeGrid>

## Skrypty PHP — rozwiązanie krok po kroku

### Połączenie z bazą

<ExamCodeGrid>
  <ExamCodeBlock
    language="PHP"
    title="Kod PHP"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`<?php
$conn = mysqli_connect("localhost", "root", "", "matura");
mysqli_query($conn, "SET CHARSET utf8");`}
  />
</ExamCodeGrid>

### Skrypt 1 — 4 bloki statystyk (index.php i wynik.php)

<ExamCodeGrid>
  <ExamCodeBlock
    language="PHP"
    title="Kod PHP"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`// Blok Przedmioty
$wynik = mysqli_query($conn, "SELECT DISTINCT przedmiot FROM arkusz");
echo "<div class='blok'><h4>Przedmioty</h4>";
while ($row = mysqli_fetch_array($wynik)) {
    echo $row['przedmiot'] . " ";
}
echo "</div>";

// Blok Lata
$wynik = mysqli_query($conn, "SELECT MIN(rok), MAX(rok) FROM arkusz");
$row = mysqli_fetch_array($wynik);
echo "<div class='blok'><h4>Lata</h4>" . $row[0] . " - " . $row[1] . "</div>";

// Blok Najlepszy wynik
$wynik = mysqli_query($conn,
    "SELECT AVG(punkty) AS Wynik FROM wyniki
     GROUP BY id_maturzysta ORDER BY Wynik DESC LIMIT 1");
$row = mysqli_fetch_array($wynik);
echo "<div class='blok'><h4>Najlepszy wynik</h4>" . round($row['Wynik']) . "%</div>";

// Blok Najgorszy wynik
$wynik = mysqli_query($conn,
    "SELECT AVG(punkty) AS Wynik FROM wyniki
     GROUP BY id_maturzysta ORDER BY Wynik ASC LIMIT 1");
$row = mysqli_fetch_array($wynik);
echo "<div class='blok'><h4>Najgorszy wynik</h4>" . round($row['Wynik']) . "%</div>";`}
  />
</ExamCodeGrid>

### Skrypt 2 — lista uczniów jako odnośniki (index.php)

<ExamCodeGrid>
  <ExamCodeBlock
    language="PHP"
    title="Kod PHP"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`echo "<h3>Wybierz ucznia z listy:</h3>";
$wynik = mysqli_query($conn,
    "SELECT id, imie, nazwisko FROM maturzysta WHERE szkola = 'T3' ORDER BY nazwisko ASC");
while ($row = mysqli_fetch_array($wynik)) {
    echo "<a href='wynik.php?id=" . $row['id'] . "'>"
         . $row['imie'] . " " . $row['nazwisko'] . "</a><br>";
}`}
  />
</ExamCodeGrid>

### Skrypt 3 — wyniki wybranego maturzysty (wynik.php)

<ExamCodeGrid>
  <ExamCodeBlock
    language="PHP"
    title="Kod PHP"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`if (isset($_GET['id'])) {
    $id = $_GET['id'];
    $wynik = mysqli_query($conn,
        "SELECT a.rok, a.sesja, a.przedmiot, w.punkty
         FROM wyniki w JOIN arkusz a ON w.id_arkusz = a.id
         WHERE w.id_maturzysta = $id");

    echo "<table><tr><th>Rok</th><th>Sesja</th><th>Przedmiot</th><th>Punkty</th></tr>";
    while ($row = mysqli_fetch_array($wynik)) {
        echo "<tr><td>" . $row['rok'] . "</td><td>" . $row['sesja'] . "</td>"
             . "<td>" . $row['przedmiot'] . "</td><td>" . $row['punkty'] . "</td></tr>";
    }
    echo "</table>";
}`}
  />
</ExamCodeGrid>

### Zamknięcie

<ExamCodeGrid>
  <ExamCodeBlock
    language="PHP"
    title="Kod PHP"
    description="Kod z rozwiązania arkusza INF.03."
    mistake="Sprawdź nazwy plików, pól, tabel i zgodność z poleceniem."
    code={`mysqli_close($conn);
?>`}
  />
</ExamCodeGrid>

## Minimum do zapamiętania z PHP

- Po `mysqli_connect()` ustaw kodowanie (`SET CHARSET utf8` albo odpowiednik z arkusza).
- Najpierw testuj SQL w bazie, potem wklejaj do PHP.
- Dane z formularza czytaj przez `$_POST`/`$_GET` zgodnie z poleceniem.
- `while + mysqli_fetch_array/fetch_assoc` to podstawowy wzorzec renderowania wyników.
- Zamknij połączenie, jeśli arkusz tego wymaga.
